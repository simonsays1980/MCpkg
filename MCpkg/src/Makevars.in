PKG_LIBS = -lgomp
PKG_CPPFLAGS = -g3 -fopenmp -DSCYTHE_DEBUG=3 -DSCYTHE_COMPILE_DIRECT -DSCYTHE_RPACK -DSCYTHE_PTHREAD -pthread -DHAVE_TRUNC @MV_HAVE_IEEEFP_H@ @MV_HAVE_TRUNC@

OBJECTS.include.scythestat.rng = include/scythestat/rng/lecuyer.o
OBJECTS.root = MCgmmS.o

OBJECTS = $(OBJECTS.include.scythestat.rng) $(OBJECTS.root)

all: before $(SHLIB) after

clean:	
	rm -f $(OBJECTS)

before: clean 

after: 

